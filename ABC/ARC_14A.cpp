#include <bits/stdc++.h>
using namespace std;
using Graph = vector<vector<int>>;

int main() {
  int N;
  cin >> N;

  vector<string> A(N);
  for (int i = 0; i < N; i++) {
    cin >> A.at(i);
    if (i >= 1) {
    }
  }