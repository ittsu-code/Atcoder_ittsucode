#include <bits/stdc++.h>
using namespace std;
using Graph = vector<vector<int>>;

int main() {
  int N, M, L, P, Q, R;
  cin >> N >> M >> L >> P >> Q >> R;

  vector<int> box = {N, M, L};
  vector<int> bagede = {P, Q, R};

  int ans = 0;
}